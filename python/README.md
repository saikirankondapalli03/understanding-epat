# Python versions of the lecture programs

Same jobs Chan assigned, in pandas/numpy. Toy numbers are included so each
file runs without the original data files.

```
pip install numpy pandas scipy
```

Then from this folder:

```
python gld_gc.py
python xle_uso_roll.py
python vx_es.py
python correlation_test.py
python hurst_vratio.py
python tu_mom.py
python cl_rev.py
python kentdaniel.py
python pead.py
```

Read them in that order — it matches notes 04 → 11.
